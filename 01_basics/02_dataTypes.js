"use strict"; // treats all js code as newer js version

// alert ("hello");
// alert (3 + 3);   we are using nodejs not browser

console.log(3 
    + 3) // code will run here but this is not the correct way to write the code.. code should be readable.
console.log(3 + 3); // this is proper way to write code

let name = "Nitesh"
let age = 35
let isLoggedIn = false
let state;

// number = 2 to power 53
// bigint = for large numbers 
// string = "assembly of characters"
// boolean = true/false
// null = stand alone value (type = object)
// undefined = kinda place holder for a value which is not defined yet (type = undefined) 
// symbol = uniqueness
// NaN (not a number) = special type

// object

console.log(typeof "Nitesh");
console.log(typeof null); // type = object
console.log(typeof undefined); // type = undefined
